# Bitácora de sesión — 28 mayo 2026

## Estado al cerrar
**CI 100% verde por primera vez** (commit c1c8e22): backend, frontend y smoke test, los tres jobs en verde.

## Cerrado hoy
- **Auditoría de telemetría COMPLETA** — los 5 hallazgos cerrados:
  - #1 Backlog Redis (MAXLEN) — sesión previa
  - #2 received_at en telemetry_record — migración 031
  - #3 Catálogo AVL + log de IDs desconocidos — _KNOWN_AVL_IDS / _logged_unknown_avl
  - #4 Codec 12 parser — branch propio antes de decode_packet (commit eca1d16), validado en producción con IMEI 862272089079729: publish → envío → ACK → command_log=confirmed
  - #5 Campos de salud en device (last_packet_at, total_messages, last_codec, iccid) — migración 031
- **CI reparado de punta a punta:**
  - Fix UPLOADS_DIR portable + mkdir en lifespan (commit c5b67b3) — desbloqueó los 36 tests de backend
  - 11 tests de frontend anticuados actualizados (commit 40dc15e) — solo *.test.tsx
  - 11 tests de backend anticuados actualizados (commit c1c8e22) — solo test_*.py
  - Todos eran Categoría A (tests atrasados tras refactors), ningún bug real de producción

## Para la próxima — por orden de importancia
1. **REDISEÑO VISUAL (plato fuerte).** Adoptar el design-system de `temp/design-system`: paleta "sala de control fría" (azul-grises) en vez de la "industrial cálida" actual (marrones), + teal #1D9E75, + tipografía Inter. Es trabajo de VARIOS DÍAS. Hacerlo POR CAPAS de abajo arriba: 1º tokens, 2º componentes átomos, 3º pantallas una a una, verificando que el CI sigue verde tras cada capa. NO meter los componentes JSX del kit (son prototipos simplificados, no producción). El kit es REFERENCIA visual + un SKILL.md para que Code genere mocks on-brand.
2. **GAP 1 — Esquemático en vivo** (plan WECOVE_PARITY). Dibujo SVG del camión cuba VPS con tanque/válvulas/bomba/manómetros animados en tiempo real. Todo listo para arrancar: datos llegan, WebSocket (`ws.py` + `wsClient.ts`) ya existe, sensor_schema ya mapea. Falta solo el componente `VehicleSchematicView.tsx`. Empezar por el tanque. Parte de cero en código.

## Cabos menores anotados
- **avl_380 duplicado / solapamiento 10309-10315** en sensor_schema de vacío-presión — revisar (no diagnosticado a fondo aún; sospecha de un AVL ID mapeado a dos señales)
- **CLAUDE.md desactualizado**: documenta naranja #F97316, pero producción ya usa teal. Corregir al tocar diseño.
- **3 warnings "coroutine never awaited"** en test_devices_api.py — preexistentes, menores
- **Deprecación Node 20** en GitHub Actions — afecta jun/sep 2026, sin urgencia

## Notas de método (lo que funcionó)
- Mirar antes de tocar; diagnóstico antes que arreglo
- Commits aislados, uno por asunto; push solo respalda (no despliega)
- Para tests rotos: clasificar Categoría A (test anticuado) vs B (bug real) ANTES de arreglar; nunca tocar producción para hacer pasar un test
